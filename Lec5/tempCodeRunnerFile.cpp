    // buildPrefTree(&root, prefeq, &pos);
    

    // inOrder(root);
    // printf("\n");

    // postorder(root);
    // printf("\n");

    // preorder(root);
    // printf("\n");

    // int res = evaluate(root);
    // printf("== %d\n", res);

    // tnode* postTree = buildPostTree(postfeq);
    // inOrder(postTree);